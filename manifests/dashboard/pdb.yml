---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dashboard
  namespace: beacon
  labels:
    app: dashboard
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: dashboard
